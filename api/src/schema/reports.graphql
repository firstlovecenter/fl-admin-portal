type DirectoryReportEntry @node{
  id: ID!
  level: String!
  name: String!
  parentName: String
  leaderName: String
  leaderPhone: String
  leaderWhatsApp: String
}

type WeeklyChurchReportEntry @node{
  id: ID!
  churchId: ID!
  churchName: String!
  churchLevel: String!
  week: Int!
  year: Int!
  serviceAttendance: Int
  serviceIncome: Float
  serviceDollarIncome: Float
  numberOfServices: Int
  bussingAttendance: Int
  bussingLeaderDeclaration: Int
  numberOfSprinters: Int
  numberOfUrvans: Int
  numberOfCars: Int
  bussingTopUp: Float
}

type ServiceTreasurerEntry @node{
  id: ID!
  name: String!
  phone: String
  whatsapp: String
}

"""
One row per `ServiceRecord` (including `:NoService` markers). Used by the
Bacenta-scope Weekday detail report — higher levels stay on the weekly
aggregate shape (`WeeklyChurchReportEntry`) because per-record granularity
explodes the row count.
"""
type ServiceRecordDetail @node{
  id: ID!
  churchId: ID!
  churchName: String!
  serviceDate: String
  week: Int
  year: Int
  attendance: Int
  income: Float
  cash: Float
  onlineGiving: Float
  numberOfTithers: Int
  dollarIncome: Float
  foreignCurrency: String
  noServiceReason: String
  createdAt: String
  recordedByName: String
  recordedByPhone: String
  treasurers: [ServiceTreasurerEntry!]!
  familyPicture: String
  treasurerSelfie: String
  bankingSlip: String
  transactionStatus: String
  bankingProof: Boolean
  bankedByName: String
  bankedByPhone: String
}

extend type Bacenta {
  directoryReport: [DirectoryReportEntry!]!
  servicesHeldReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayIncomeBussingReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayServiceRecordsReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [ServiceRecordDetail!]!
  subChurchesReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
}

extend type Governorship {
  directoryReport: [DirectoryReportEntry!]!
  servicesHeldReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayIncomeBussingReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  subChurchesReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
}

extend type Council {
  directoryReport: [DirectoryReportEntry!]!
  servicesHeldReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayIncomeBussingReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  subChurchesReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
}

extend type Stream {
  directoryReport: [DirectoryReportEntry!]!
  servicesHeldReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayIncomeBussingReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  subChurchesReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
}

extend type Campus {
  directoryReport: [DirectoryReportEntry!]!
  servicesHeldReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayIncomeBussingReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  subChurchesReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
}

extend type Oversight {
  directoryReport: [DirectoryReportEntry!]!
  servicesHeldReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  weekdayIncomeBussingReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
  subChurchesReport(
    startWeekKey: Int!
    endWeekKey: Int!
  ): [WeeklyChurchReportEntry!]!
}
