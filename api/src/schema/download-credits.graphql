extend type Fellowship {
  downloadMembership: [Member!]!
}

extend type Bacenta {
  downloadMembership: [Member!]!
}

extend type Governorship {
  downloadMembership: [Member!]!
}

extend type Council {
  downloadMembership: [Member!]!
}

extend type Stream {
  downloadMembership: [Member!]!
}

extend type Campus {
  downloadMembership: [Member!]!
}

extend type Oversight {
  downloadMembership: [Member!]!
}
